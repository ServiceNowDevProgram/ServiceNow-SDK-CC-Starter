Build and deploy the ServiceNow app to the instance.

Run the following commands in sequence:
1. `npm run build` - Compile the source files into app files
2. `npm run deploy` - Install/update the application on the ServiceNow instance

Report the result of each step. If the build fails, do not proceed to deploy.